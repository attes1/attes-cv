angular.module('cv2').directive('autoheight', function() {
    return function (scope, element, attrs) {
        //element.height($(window).height() - $('.header').outerHeight());
        console.log('autoheight');
    }
});
